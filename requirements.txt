# 前端框架（适配Python 3.13）
streamlit>=1.24.0,<2.0.0

# Oracle数据库连接（替换cx_Oracle为oracledb，无需客户端）
oracledb>=1.4.0

# 数据分析与可视化（修复calmap版本，适配Python 3.13）
pandas>=2.0.0,<2.4.0  # 避免高版本兼容问题
numpy>=1.24.0,<2.0.0
matplotlib>=3.7.0,<3.11.0
seaborn>=0.12.2,<0.14.0
calmap>=0.0.11  # calmap最高稳定版本，修正错误的0.1.9

# AI模型调用（百炼SDK，适配Python 3.13）
aiohttp>=3.8.4,<3.11.0
requests>=2.31.0,<2.32.0
dashscope>=1.14.0,<2.0.0  # 替换原pip命令，仅保留包名+版本

# 工具库（兼容Python 3.13）
python-dotenv>=1.0.0,<2.0.0
pydantic>=2.0.0,<2.8.0
loguru>=0.7.0,<0.8.0
setuptools>=65.5.0,<70.0.0

# 测试库（部署时可选，建议注释/删除以减少依赖）
# pytest>=7.3.0
# pytest-mock>=3.10.0
